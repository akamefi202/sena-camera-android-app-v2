<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SenaCamera" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/secondary_color</item>
        <item name="android:textColor">@color/white</item>
<!--        <item name="android:windowLightStatusBar">true</item>-->
        <item name="android:statusBarColor">@color/background_primary_color</item>
    </style>

    <style name="Theme.SenaCamera" parent="Base.Theme.SenaCamera" />
</resources>